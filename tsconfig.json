{
  "compilerOptions": {
    "target": "es6", // 指定ECMAScript目标版本
    "module": "commonjs", // 指定模块化类型
    "declaration": true, // 生成 `.d.ts` 文件
    "outDir": "./dist", // 编译后生成的文件目录
    "strict": true // 开启严格的类型检测
  },
  // "files": [], // 指定包含哪些文件
  "include": [ // 只编译哪些目录下的ts文件 使用"include"引入的文件可以使用"exclude"属性过滤。
    "src/**/*",
    "lib/**/*",
  ],
  "exclude": [ // "exclude"默认情况下会排除node_modules，bower_components，jspm_packages和<outDir>目录
    "node_modules",
  ]
}